export function DemoModeBanner() {
  return (
    <div className="fixed top-0 left-0 right-0 z-50 bg-warning text-slate-900 text-center py-2 px-4 text-sm font-semibold">
      ⚠ DEMO MODE — Using simulated data. No real payments affected.
    </div>
  )
}
